<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fitness2020channelapp">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.OrderActivity"></activity>
        <activity android:name=".Activities.OfferingScheduleActivity" />
        <activity android:name=".Activities.EventsActivity" />
        <activity android:name=".Activities.ReviewsActivity" />
        <activity android:name=".Activities.VouchersActivity" />
        <activity android:name=".Activities.OfferingsActivity" />
        <activity android:name=".Activities.ActivitiesFacilitiesActivity" />
        <activity android:name=".Activities.InfoActivity" />
        <activity android:name=".Activities.ManagerActivity" />
        <activity android:name=".Activities.PhotoVideoActivity" />
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>